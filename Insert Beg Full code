#include <stdio.h>
#include<stdlib.h>

struct node {
    int data;
    struct node*prev;
    struct node*next;
}*header;

void createlist (int n);
void insertatbeg();
void traverse(struct node*header);


int main () {
    int n;
    printf("Enter the num of node: ");
    scanf("%d",&n);
    createlist (n);

insertatbeg();
    printf("Double linked list:\n");
    traverse(header);
  
} 

void createlist(int n){
    int data;
    struct node*newnode,*temp;
    newnode=(struct node*)malloc(sizeof(struct node));
    if(newnode==NULL) {
        printf("unable");
    }
    else{
        printf("1 no nodes data: ");
        scanf("%d",&data);
        newnode->data=data;
        newnode->prev=NULL;
        newnode->next=NULL;
        header=newnode;
        temp=newnode;
        
        for(int i=2;i<=n;i++) {
            newnode=(struct node*)malloc(sizeof(struct node));
    if(newnode==NULL) {
        printf("unable");
        break;
    }
    else{
        printf("%d no nodes data: ",i);
        scanf("%d",&data);
        newnode->data=data;
        newnode->prev=temp;
        newnode->next=NULL;
        temp->next=newnode;
        temp=temp->next;
        }
    }
}
}

void insertatbeg() {
    struct node*newnode;
    newnode=(struct node*)malloc(sizeof(struct node));
    printf("Enter data to insert at beg: ");
    scanf("%d",&newnode->data);
    
    newnode->prev= NULL;
    newnode->next= header;
    if(header!=NULL) {
header->prev=newnode;
    }
header=newnode;
   printf("Inserted successfully\n") ;
}

void traverse(struct node*header){
    struct node*temp;
    if(header==NULL) {
        printf("List is empty");
    }
    else {
        temp=header;
        while(temp!=NULL) {
            printf("%d <--> ",temp->data);
            temp=temp->next;
        }
        printf("NULL");
    }
}
