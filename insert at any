insertatany(int data,int pos) {
    struct node*newnode,*temp;
    newnode=(struct node*)malloc(sizeof(struct node));
    if(newnode==NULL) {
        printf("unable");
    }
    else {
newnode->data=data;
newnode->next=NULL;
newnode->prev=NULL;
if(pos==1) {
    newnode->prev=NULL;
    newnode->next=header;
    header=newnode;
}
else{
    temp=header;
    for(int i=2;i<=pos-1;i++) {
        temp=temp->next;
        if(temp==NULL) {break;}
    } if(temp!=NULL) {
        newnode->next=temp->next;
        temp->next=newnode;
    } else{
        printf("cannot");
    }
}
        
        
    }  }
